# Coverage configuration for DICOM Viewer V3.
# CI: PYTHONPATH=src python -m pytest tests --cov=src --cov-report=term --cov-fail-under=80
# Local: python -m pytest tests --cov=src --cov-report=html --cov-report=term --cov-fail-under=80
# Coverage reports stay local to the developer machine or CI job log and are
# not uploaded to third-party analysis services. The 80% floor is enforced in
# CI (not on local pre-push).

[run]
source = src
branch = True
relative_files = True
omit =
    src/__pycache__/*
    src/**/__pycache__/*
    # Generated/extracted third-party bundled inside src/ (none currently, guard for future)
    src/_vendor/*
    # Side-effect-only entry point; exercising it needs a full GUI launch.
    src/main.py
    # Extracted mixin modules (omitted until coverage debt paid down; see MAIN_PY_REFACTOR_PLAN).
    src/main_app_*.py

[report]
# Regex exclude — skip __debug__/unreachable branches and typing-only blocks
exclude_lines =
    pragma: no cover
    def __repr__
    raise AssertionError
    raise NotImplementedError
    if __name__ == .__main__.:
    if TYPE_CHECKING:
    @abstractmethod
show_missing = False
skip_covered = False
sort = cover

[xml]
output = coverage.xml
