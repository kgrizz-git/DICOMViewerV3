# DICOM Viewer V3 Requirements
# 
# This file lists all Python package dependencies required for the DICOM viewer application.
# 
# Installation: pip install -r requirements.txt
#
# Requirements:
# - PySide6: Cross-platform GUI framework for the application interface
# - pydicom: Library for reading, writing, and working with DICOM files
# - numpy: Numerical computing library for image array operations
# - Pillow: Image processing library for image format conversions
# - matplotlib: Plotting library for histogram display
# - openpyxl: Library for reading and writing Excel files (for tag export)

PySide6>=6.6.0
pydicom>=2.4.0
numpy>=1.24.0
Pillow>=10.0.0
matplotlib>=3.8.0
openpyxl>=3.1.0
SimpleITK>=2.3.0  # For 3D image resampling in image fusion (Phase 2)

# Optional dependencies for compressed DICOM support
# These are recommended for decoding JPEG-compressed DICOM files (JPEG, JPEG-LS, JPEG 2000)
# Required for lossyJPEG-YBR_FULL_422 and other compressed transfer syntaxes
# Note: GDCM support may require additional system libraries and is not included here
pylibjpeg>=1.4.0  # For JPEG 2000 and other compressed formats
pylibjpeg-libjpeg>=1.3.0  # JPEG decoder plugin for pylibjpeg (required for JPEG Baseline/Extended)
pylibjpeg-openjpeg>=1.3.0  # JPEG 2000 decoder plugin for pylibjpeg
pylibjpeg-rle>=1.3.0  # RLE decoder plugin for pylibjpeg
pyjpegls>=0.3.0  # For JPEG-LS compression

