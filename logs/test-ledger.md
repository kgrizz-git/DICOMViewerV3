# Test ledger

Append or update rows (**newest at top**). Per `test-ledger-runner`, keep **≤5** latest runs per suite/command group.

| Suite / command | Date | Tests related (files) | Last result | Notes |
|-----------------|------|------------------------|-------------|-------|
| `python -m pytest tests/test_multi_window_layout.py tests/test_deep_anonymizer.py -v` (venv `.venv`, win32, Python 3.12.10, pytest 9.0.3) | 2026-06-04 | **LP-A** `tests/test_multi_window_layout.py` (10); **LP-B** `tests/test_deep_anonymizer.py` (7) | **17 passed** (~2.5 s) | Targeted fan-in after full suite green. **LP-A:** 3-pane grid cells, cycle order, 2-pane toggle. **LP-B:** profile, PS3.15 tags, UID remap, date shift, private strip, manufacturer retain, pydicom round-trip. **0** failed / **0** skipped. |
| `python -m pytest tests/ -v` (venv `.venv`, win32, Python 3.12.10, pytest 9.0.3) | 2026-06-04 | Full `tests/` (**856** collected; **Dual P1 LP-A+LP-B** slice-end **`medium`** gate) | **856 passed** (~82 s) | Post-**coder** fan-in **LP-A** (three-pane layouts) + **LP-B** (deep anonymizer export). **0** failed / **0** skipped / **0** flaky. **21** warnings (pydicom invalid VR UI in study-cache tests — existing). **`test_multi_window_layout.py`** (10) + **`test_deep_anonymizer.py`** (7) exercised in full tree. **Suggested manual smoke (UX):** keys **2**/**3**/**4** layout cycle (1×2↔2×1, 3-pane cycle, 2×2); **File → Export → Export with Deep Anonymization…** — dialog defaults all-on, export CT/MR sample, reload confirm institution/patient stripped. |
| `python -m pytest tests/ -v` (venv `.venv`, win32, Python 3.12.10, pytest 9.0.3) | 2026-06-04 | Full `tests/` (**819** collected; user-requested full suite) | **819 passed** (~103 s) | **Pre-run env:** first collect **ERROR** `tests/test_pylinac_extent_subclasses.py` — `ModuleNotFoundError: annotated_types` (pydantic/pylinac); also missing **`argue`** (pylinac). Repaired with `pip install annotated-types` + `pip install -r requirements.txt` (no product/test edits). **0** failed / **0** skipped. **22** warnings (pydicom invalid VR UI in study-cache tests; `requests` charset hint — existing patterns). User will run manual smoke separately. |
| `python -m pytest tests/ -v` (venv `.venv`, win32, Python 3.13.13, pytest 9.0.2) | 2026-04-17 | Full `tests/` (**513** collected; **CINE1-FU** slice-end gate) | **513 passed** (~97 s) | Post-**coder** **CINE1-FU** (codecs AVI mpeg4/yuv420p, MPG MPEG-2, GIF FPS helper, cine export dialog scale parity, wiring, title case, **`tests/test_cine_video_export.py`**). **0** failed / **0** skipped / **0** flaky retries. Warnings: SWIG `DeprecationWarning` x3 (`<frozen importlib._bootstrap>:488`; existing). **Suggested manual smoke (UX):** **File → Export Cine As…** title/dialog copy; export **AVI**/**MPG**/**GIF** and spot-check **WMP**/**GIF** viewer; confirm scale options align with static image export. |
| `python -m pytest tests/ -v` (venv `.venv`, win32, Python 3.13.12, pytest 9.0.2) | 2026-04-15 | Full `tests/` (**468** collected; **CINE1** gate) | **468 passed** (~69 s) | Independent full suite on **`feature/cine-video-export`** after coder. **0** failed / **0** skipped. **`tests/test_cine_video_export.py`** exercised in full tree. Warnings: SWIG `DeprecationWarning` x3 (`<frozen importlib._bootstrap>:488`; existing). |
| `python -m pytest tests/ -q` (venv `.venv`, win32, Python 3.13.12, pytest 9.0.2) | 2026-04-14 | Full `tests/` | **439 passed** (~41 s) | Post-fix: `tests/test_main_window_theme.py` RGB expectations aligned with `get_theme_viewer_background_color` (**#0e0e0e** dark, **#262626** light/unknown). **`tests/test_mpr_dicom_export.py`** still green. SWIG `DeprecationWarning` x3 (existing). |
| `python -m pytest tests/ -v` (venv `.venv`, win32, Python 3.13.12, pytest 9.0.2) | 2026-04-14 | Full `tests/` | **3 failed, 436 passed** (~53 s) | **First failure:** `tests/test_main_window_theme.py::TestGetThemeViewerBackgroundColor::test_dark_theme_returns_expected_color` — `result.red()` **14** vs expected **27**. Same class: light/unknown theme tests expect **64**, got **38** (`test_light_theme_returns_expected_color`, `test_unknown_theme_returns_light_viewer_color`). Likely **`main_window_theme.py`** palette vs frozen test expectations (same pattern as 2026-04-14 coder note). **`tests/test_mpr_dicom_export.py`:** not in failure list (full collect **439** items). Warnings: SWIG `DeprecationWarning` x3 from imports (existing). |
