# Test ledger

Append rows per skill **`test-ledger-runner`** (newest entries at top). The **tester** subagent maintains this file; other roles do not use it to “fix” failing tests.

| Suite / command | Date | Tests related (files) | Last result | Notes |
|-----------------|------|------------------------|-------------|-------|
