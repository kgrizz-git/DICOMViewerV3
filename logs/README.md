# Logs

- **`test-ledger.md`** — append-only test run history from the **tester** subagent (see skill `test-ledger-runner`).
- **`docs_log-*.md`** — documentation review outputs from **docreviewer** (path chosen per that agent’s rules if not here).

Long-running **multi-agent** coordination lives under **`plans/orchestration-state.md`**, not in this folder.
