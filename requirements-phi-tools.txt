# Optional PHI/PII scanning environment.
#
# Do not install alongside requirements.txt or requirements-dev.txt. Presidio
# Analyzer pins NumPy <2.5 while the application requires NumPy >=2.5.1.
# PhiScan 0.7.0 requires Typer ~=0.24.1; pin its compatible Typer explicitly.
#
# Create a dedicated virtual environment and install this file there. Presidio
# image redaction additionally requires a locally installed OCR engine and an
# appropriate spaCy model.
phi-scan==0.7.0; python_version >= "3.12"
typer==0.24.1; python_version >= "3.12"
presidio-analyzer==2.2.363
presidio-image-redactor==0.0.59
dicom-phi-scan @ git+https://github.com/elijahrockers/dicom-phi-scan.git@e558a5232f405b4065d6079605a853f3e7c10152
