version: 2
updates:
  # Python dependencies (pip)
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "03:00"
    open-pull-requests-limit: 10
    cooldown:
      default-days: 7
    reviewers:
      - "GitHub-Copilot"
    labels:
      - "dependencies"
    commit-message:
      prefix: "ci"
      include: "scope"
    pull-request-branch-name:
      separator: "/"
    groups:
      # Bundle routine patch/minor bumps into one weekly PR; majors (e.g. a
      # pydicom 2->3 jump) still open individually since they need review.
      # pylinac needs ACR QA re-verify; typer is pinned for phi-scan ~=0.24.1 —
      # both stay ungrouped so they get solo PRs instead of riding with safe bumps.
      python-patch-minor:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"
        exclude-patterns:
          - "pylinac"
          - "typer"

  # GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "05:00"
    open-pull-requests-limit: 5
    cooldown:
      default-days: 7
    labels:
      - "dependencies"
      - "github-actions"
    commit-message:
      prefix: "ci(actions):"
    groups:
      actions-patch-minor:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"
  # Docker: add a `docker` package-ecosystem block here only after a Dockerfile
  # exists at the repo root (or set directory to the folder that contains it).
  # Dependabot errors if `docker` is enabled without a matching Dockerfile.
