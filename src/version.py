"""
Application version (semantic versioning).

This module is the single source of truth for the DICOM Viewer V3 application
version. Update __version__ when releasing; use the same value for Git tags (with
a "v" prefix, e.g. v3.0.0).

See dev-docs/info/SEMANTIC_VERSIONING_GUIDE.md for version increment rules.
"""

__version__ = "0.1.10"
