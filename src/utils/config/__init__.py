"""
utils.config package

Contains feature-domain mixin classes that are composed by ConfigManager.
Each mixin owns the get/set methods for one area of application settings.
Import ConfigManager from utils.config_manager, not from this package directly.
"""
